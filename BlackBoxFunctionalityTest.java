public class BlackBoxFunctionalityTest
{
    //Verifies app displays all spaces with busyness level on home screen
     public void homeScreenDisplaysStudySpaces()
     {
        //Test code to verify that the home screen displays all study spaces with their busyness levels
     }

     //Verifies selected study space displays its data 
     public void studySpaceDataDisplays()
     {

     }

     //Verifies noise level filtering returns spaces with matching input
     public void filterByNoiseLevelDisplaysMatches()
     {

     }

     //Verifies outlet availability filtering returns spaces with matching input
     public void filterByOutletAvailabilityDisplaysMatches()
     {

     }

     //Verifies a submitted report updates the study space's busyness and noise data
     public void submittedBusynessAndNoiseReportUpdatesSpaceData()
     {

     }

     //Verifies a partially complete report is still accepted and only updates the reported part
     public void partialReportAcceptedAndUpdates()
     {

     }

     //Verifies a message is displayed when no study spaces match the filter criteria 
     public void noFilterMatchDisplaysMessage()
     {

     }

     //Verifies the progess bar and color correctly match the displayed busyness level
     public void progressBarAndColorMatchBusyness()
     {

     }

     //Verifies selecting the map button navigates to the map section
     public void mapSelectionNavigatesToMap()
     {

     }

     //Verifies selecting history button navigates to history + favorites section 
     public void historySectionNavigatesToHistoryAndFavorites()
     {

     }
}